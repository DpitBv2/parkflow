import { StyleSheet, Text, View } from "react-native";

const AboutUs = () => {
    return (
        <View>
            <Text>AboutUs</Text>
        </View>
    );
};

const styles = StyleSheet.create({});

export default AboutUs;
