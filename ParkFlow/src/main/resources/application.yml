spring:
  spring:
    main:
      web-application-type: none
  datasource:
    url: jdbc:postgresql://localhost:5432/parkflow
    username: postgres
    password: alexmoto
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        show_sql: true
        format_sql: true
    hibernate:
      ddl-auto: update
  flyway:
    baseline-on-migrate: true
    url: jdbc:postgresql://localhost:5432/parkflow
    user: postgres
    password: alexmoto
    locations: classpath:src/main/resources/db/migrations