import { StyleSheet, Text, View } from "react-native";
const Settings = () => {
    return (
        <View>
            <Text>Settings</Text>
        </View>
    );
};

const styles = StyleSheet.create({});

export default Settings;
